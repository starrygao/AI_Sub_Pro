# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.coverage
.venv/
venv/

# Build artifacts (PyInstaller / Electron)
build/
dist/
*.spec.bak

# macOS
.DS_Store

# Editor / IDE
.idea/
.vscode/
*.swp
.worktrees/

# Local data (user runtime data lives under ~/AI_Sub_Pro_Data in bundled builds;
# in dev mode it sits in ./data — keep config.json & knowledge.json out of git)
data/projects/
data/config.json
data/config.invalid-*.json
data/knowledge.json
data/knowledge.v1.backup.json

# Local packaged ASR model cache (used by build_mac.sh when present)
models/asr/

# Logs
*.log
